<div class="row">
  <% @users.each do |user| %>
    <h1><%= user.name %></h1><br>
    <!--- <div class="row">
      <% user.windows.each do |window| %>
      <div class="col-md-3">
      <%= image_tag window.image.url(:medium) %><br>
      <h4><%=  window.name %><h4>
      </div>
      <% end %>
    </div> --->
<% end %>


</div>
